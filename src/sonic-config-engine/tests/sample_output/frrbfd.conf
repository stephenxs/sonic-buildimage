!
! =========== Managed by sonic-cfggen DO NOT edit manually! ====================
! generated by templates/frr/frr.conf.j2 with config DB data
! file: frr.conf
!
!
password zebra
log syslog informational
log facility local4
agentx
! enable password !
! Enable link-detect (default disabled)
!
! set static default route to mgmt gateway as a backup to learned default
!
! Set ip source to loopback for bgp learned routes
route-map RM_SET_SRC permit 10
    set src None
!
ip protocol bgp route-map RM_SET_SRC
!
!
!
!
route-map set-next-hop-global-v6 permit 10
set ipv6 next-hop prefer-global
!
bfd
  peer 2.2.2.2 
    transmit-interval 400 
    detect-multiplier 5
    echo-mode
    echo-interval 44
    label mylabel
    shutdown
  !
  peer 3.3.3.3 local-address 1.1.1.1 
    shutdown
  !
  peer 4.4.4.4 interface Ethernet0 
    shutdown
  !
  peer 5.5.5.5 local-address 1.1.1.1 interface Ethernet0 
    no shutdown
  !
!

